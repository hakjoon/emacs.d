;; Setting related to window management

;; winner-mode
(winner-mode t)

;; windmove bindings on
(windmove-default-keybindings)

;; winring
(require 'winring)
(winring-initialize)

(provide 'win-mgmt-cfg.el)